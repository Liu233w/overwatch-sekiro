/*
The mark of certain state, to decouple the certain kind of state with the state number, which
can be different for different heros.
*/

define playervar firingPrimary;

macro InFiringPrimaryState(define player): player.firingPrimary == true;

method SetInFiringPrimaryState(define player, define state)
{
    player.firingPrimary = state;
}